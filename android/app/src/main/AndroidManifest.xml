<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Sprint 6 (spec.md §25, spec-1.md §B.8): biometric unlock is OPT-IN and
         local-only — it releases the Keystore-wrapped vault key, never
         authenticating against the backend. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- spec-1.md §A.4: V0.1 talks plain HTTP to the homelab server (user-supplied
         LAN URL). By key design (§A.1) only auth_hash + ciphertext cross the wire;
         documented trade-off, HTTPS option lands in V0.2+. Hence
         android:usesCleartextTraffic="true" below. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VeilKeepers"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
