# Veil Keepers — Sprint 1 infrastructure.
# MySQL 8.4 + Go API. Secrets come from .env (see .env.example); every
# value below has a safe interpolation default so compose works standalone.

services:
  veilkeepers-mysql:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-change-me-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-veilkeepers}
      MYSQL_USER: ${MYSQL_USER:-veilkeepers}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-change-me-app}
    volumes:
      - mysql-data:/var/lib/mysql
      - ./infra/mysql/conf/veilkeepers.cnf:/etc/mysql/conf.d/veilkeepers.cnf:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u $$MYSQL_USER -p$$MYSQL_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

  veilkeepers-api:
    build: ./backend
    depends_on:
      veilkeepers-mysql:
        condition: service_healthy
    ports:
      - "${VK_HOST_PORT:-18080}:${VK_PORT:-8080}"
    environment:
      VK_PORT: ${VK_PORT:-8080}
      VK_DB_DSN: ${MYSQL_USER:-veilkeepers}:${MYSQL_PASSWORD:-change-me-app}@tcp(veilkeepers-mysql:3306)/${MYSQL_DATABASE:-veilkeepers}
      REGISTRATION_OPEN: ${REGISTRATION_OPEN:-true}
      VK_ATTACHMENT_DIR: ${VK_ATTACHMENT_DIR:-/data/attachments}
      VK_ATTACHMENT_MAX_BYTES: ${VK_ATTACHMENT_MAX_BYTES:-10485760}
    # Attachment ciphertext lives on the host filesystem, never in MySQL.
    # The container runs as distroless nonroot (uid/gid 65532), so the host
    # directory must be owned by 65532:65532 or startup fails the probe:
    #   mkdir -p ./data/attachments && sudo chown -R 65532:65532 ./data/attachments
    volumes:
      - ./data/attachments:/data/attachments
    healthcheck:
      test: ["CMD", "/veilkeepers-api", "-healthcheck"]
      interval: 10s
      retries: 5
      start_period: 40s
    restart: unless-stopped

volumes:
  mysql-data:
